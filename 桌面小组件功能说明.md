# 桌面小组件功能 - 文件清单与迁移指南

本文件记录「桌面小组件（App Widget）」功能涉及的所有文件，方便后续迁移到其他项目。

功能包含：
- 2×2 小组件
- 2×4 小组件（右侧 4 个可配置菜单按钮）
- App 内设置页（配置 4 个菜单分别打开哪个功能）

---

## 一、新增文件（可直接复制）

### Java（`appwidget` 包）
| 文件 | 作用 |
|------|------|
| `app/src/main/java/com/example/myapplication/appwidget/SmallWidgetProvider.java` | 2×2 组件逻辑（更新/刷新/点击打开 App） |
| `app/src/main/java/com/example/myapplication/appwidget/WideWidgetProvider.java` | 2×4 组件逻辑（4 菜单渲染、深链跳转、`refreshAll()`） |
| `app/src/main/java/com/example/myapplication/appwidget/WidgetFunctions.java` | 菜单可指向的功能定义（id/名称/图标/Tab 下标） |
| `app/src/main/java/com/example/myapplication/appwidget/WidgetPrefs.java` | 4 个菜单槽位的配置存储（SharedPreferences） |
| `app/src/main/java/com/example/myapplication/appwidget/WidgetSettingsActivity.java` | App 内设置页 |

### res/layout
| 文件 | 作用 |
|------|------|
| `app/src/main/res/layout/widget_small.xml` | 2×2 布局 |
| `app/src/main/res/layout/widget_wide.xml` | 2×4 布局（左信息 + 右 2×2 菜单） |
| `app/src/main/res/layout/activity_widget_settings.xml` | 设置页布局 |

### res/xml（组件元数据）
| 文件 | 作用 |
|------|------|
| `app/src/main/res/xml/widget_small_info.xml` | 2×2 尺寸/刷新周期等配置 |
| `app/src/main/res/xml/widget_wide_info.xml` | 2×4 尺寸/刷新周期等配置 |

### res/drawable（背景）
| 文件 | 作用 |
|------|------|
| `app/src/main/res/drawable/bg_widget.xml` | 组件渐变圆角底 |
| `app/src/main/res/drawable/bg_widget_button.xml` | 刷新按钮胶囊底 |
| `app/src/main/res/drawable/bg_widget_chip.xml` | 菜单/统计卡片底 |

---

## 二、修改的现有文件（迁移时需手动改）

| 文件 | 改了什么 | 迁移要点 |
|------|---------|---------|
| `app/src/main/AndroidManifest.xml` | 注册 2 个 `<receiver>` + `WidgetSettingsActivity`；给主 Activity 加 `android:launchMode="singleTop"` | **必须照搬**，否则组件不显示/跳转不生效 |
| `app/src/main/java/com/example/myapplication/MainActivity.java` | 新增 `EXTRA_OPEN_TAB` 常量、`onNewIntent`、`applyOpenTabIntent()`，`bottomTabBar` 提为成员字段 | 你项目导航不同的话，把「切到对应 Tab」换成你自己的跳转逻辑 |
| `app/src/main/res/values/strings.xml` | 新增 `widget_*` 文案 | 复制相关 string |
| `app/src/main/java/com/example/myapplication/data/MockDataSource.java` | 「我的-设置」列表加一行「桌面小组件设置」（带 `action=widget_settings`） | 这是**入口**，按你项目的设置页数据结构调整 |
| `app/src/main/java/com/example/myapplication/module/biz/profile/SettingListModule.java` | 设置行支持 `action` 点击 → 打开 `WidgetSettingsActivity` | 同上，入口逻辑，按你项目改 |

---

## 三、迁移到自己项目的关键注意点

1. **包名替换**：所有 `com.example.myapplication` 改成你的包名（Java 的 `package`/`import`、`R` 引用）。

2. **图标依赖**：`WidgetFunctions.java` 用了 `ic_tab_home/wealth/loan/life/profile`，布局还用了 `@mipmap/ic_launcher_round`、`@mipmap/ic_launcher`。换成你项目里的图标资源。

3. **⚠️ RemoteViews 限制（最容易踩坑）**：组件布局（`widget_small.xml`/`widget_wide.xml`）**只能用** `LinearLayout`/`FrameLayout`/`RelativeLayout`/`GridLayout` + `TextView`/`ImageView`/`Button`/`ProgressBar` 等。**不能用纯 `<View>`、`<Space>`、自定义 View、ConstraintLayout**，否则整个组件加载失败显示空白。
   - 需要占位撑高时，用空的 `<LinearLayout>` 代替 `<View>`。

4. **入口与跳转是「胶水代码」**：第二部分里 `MainActivity` / `MockDataSource` / `SettingListModule` 的改动都是为了接入**本项目特定的**导航和设置列表。迁到你项目后，这部分按你自己的页面结构重写即可，核心组件逻辑（第一部分）是通用的。

5. **核心解耦关系**（只有这 5 个 Java 文件是功能本体）：
   ```
   WidgetSettingsActivity ──写/读──> WidgetPrefs <──读── WideWidgetProvider
            │                              │                    │
            └──── 都依赖 ──> WidgetFunctions <────────────────┘
   设置页保存后调用 WideWidgetProvider.refreshAll() 刷新桌面组件
   ```

---

## 四、其他提醒

- **装新包后桌面上的旧组件不会自动更新**：改了布局或重装 App 后，需把桌面上已放置的旧组件删掉重新添加，才会套用新布局。
- **自动刷新周期**：在 `widget_*_info.xml` 的 `updatePeriodMillis` 配置（当前 30 分钟；系统最低 30 分钟，需更频繁要用 `AlarmManager`/`WorkManager`）。
- **占位数据**：两个 Provider 的 `updateAppWidget()` 里用 `=== 占位数据 ===` 注释标出了随机数/时间，替换成真实业务数据即可。
