# 音频压缩功能迁移说明

本文说明如何将当前项目的音频选择与压缩能力迁移到其他 Android 项目。

## 1. 功能与输出规格

- 通过系统文件选择器选择单个 `audio/*` 文件。
- 输入文件必须严格小于 `20 * 1024 * 1024` 字节。
- 使用 Android 系统 `MediaExtractor`、`MediaCodec` 和 `MediaMuxer`，不依赖 FFmpeg。
- 解码后转换为 22.05 kHz、单声道、16 位 PCM。
- 最终编码为 32 kbps AAC-LC，并封装为 `.m4a`。
- 默认输出到应用专属目录：`Music/compressed_audio`。
- 压缩是同步阻塞调用，必须在后台线程执行。

## 2. 环境要求

- Android `minSdk 23` 或更高版本。
- 当前代码使用 Java；目标项目可以是 Java 或 Kotlin。
- 不需要增加 Gradle 音视频依赖。
- 使用 `ACTION_OPEN_DOCUMENT` 时不需要申请存储读取权限。
- 目标设备必须提供源格式解码器和 AAC-LC 编码器。

大部分 Android 手机录音产生的 M4A/AAC、3GP/AMR、MP3、16 位 PCM WAV 均可处理。APE、WMA、24/32 位 WAV、浮点 WAV 和设备没有解码器的格式不保证支持。

## 3. 最小迁移文件

只需要压缩能力、不需要当前示例页面时，复制以下两个文件：

```text
app/src/main/java/com/example/myapplication/audio/AudioCompressor.java
app/src/main/java/com/example/myapplication/audio/AudioFileUtils.java
```

复制后修改文件顶部的 `package com.example.myapplication.audio;`，使其符合目标项目包名，并同步修改调用处的 import。

两个类的职责如下：

- `AudioFileUtils`：查询文件名和大小、处理未知文件长度、校验 20 MiB 限制、创建输出路径、格式化文件大小。
- `AudioCompressor`：提取音轨、解码、PCM 混音和降采样、AAC 编码、M4A 封装、进度回调和中断检查。

## 4. 核心调用流程

### 4.1 打开系统文件选择器

```java
private static final int REQUEST_PICK_AUDIO = 6201;

private void openAudioPicker() {
    Intent intent = new Intent(Intent.ACTION_OPEN_DOCUMENT);
    intent.addCategory(Intent.CATEGORY_OPENABLE);
    intent.setType("audio/*");
    intent.putExtra(Intent.EXTRA_ALLOW_MULTIPLE, false);
    intent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION);
    startActivityForResult(intent, REQUEST_PICK_AUDIO);
}
```

现代 AndroidX 项目可以改用 Activity Result API，但传入压缩工具的结果仍然是 `Uri`。

### 4.2 校验并压缩

以下代码必须放在线程池、协程 `Dispatchers.IO` 或其他后台任务中执行：

```java
Uri inputUri = data.getData();
if (inputUri == null) {
    throw new IOException("没有选择音频文件");
}
String mimeType = context.getContentResolver().getType(inputUri);
if (mimeType != null && !mimeType.startsWith("audio/")) {
    throw new IOException("请选择有效的音频文件");
}

AudioFileUtils.FileInfo info = AudioFileUtils.inspect(context, inputUri);
if (!AudioFileUtils.isAcceptedSize(info.size)) {
    throw new IOException("音频文件必须严格小于 20 MB");
}

File outputFile = AudioFileUtils.createOutputFile(
        context, info.displayName, System.currentTimeMillis());
File tempFile = new File(
        outputFile.getParentFile(), "." + outputFile.getName() + ".part");

try {
    new AudioCompressor().compress(context, inputUri, tempFile,
            new AudioCompressor.ProgressCallback() {
                @Override
                public void onProgress(int progress) {
                    // progress 范围为 0 到 100；切回主线程更新 UI。
                }
            });

    if (Thread.currentThread().isInterrupted()) {
        throw new InterruptedIOException("压缩已取消");
    }
    if (!tempFile.renameTo(outputFile)) {
        throw new IOException("无法保存压缩后的文件");
    }
} finally {
    if (tempFile.exists()) {
        //noinspection ResultOfMethodCallIgnored
        tempFile.delete();
    }
}
```

成功后，`outputFile` 就是压缩后的 M4A 文件。`AudioCompressor.compress()` 返回前已经完成 `MediaMuxer.stop()`，因此返回后才可以移动、上传或分享该文件。

### 4.3 取消任务

当前实现使用线程中断作为取消信号。页面销毁或用户取消时，应同时取消任务并关闭线程池：

```java
future.cancel(true);
executor.shutdownNow();
```

`AudioFileUtils` 和 `AudioCompressor` 会检查线程中断并抛出 `InterruptedIOException`。调用方应将其视为正常取消，不要显示“压缩失败”。

## 5. 迁移完整示例页面

如果需要将当前可操作页面一并迁移，还需要复制：

```text
app/src/main/java/com/example/myapplication/AudioCompressionActivity.java
app/src/main/res/layout/activity_audio_compression.xml
app/src/main/res/drawable/bg_audio_primary_button.xml
app/src/main/res/drawable/bg_audio_result_card.xml
app/src/main/res/drawable/ic_arrow_back_24.xml
```

同时迁移 `strings.xml` 中所有以 `audio_` 开头的字符串。

页面引用了以下现有颜色，目标项目可以复制对应的日间/夜间颜色，也可以替换为自己的主题资源：

```text
white
item_demo_page_background
item_demo_surface
item_text_primary
item_text_secondary
item_text_hint
item_demo_accent
item_demo_value_accent
```

当前 `AudioCompressionActivity` 继承项目专有的 `CaptureAwareActivity`。迁移时应改为目标项目使用的 Activity 基类，例如：

```java
public class AudioCompressionActivity extends AppCompatActivity {
```

不需要复制当前项目的 `security` 包。

在目标项目 `AndroidManifest.xml` 的 `<application>` 中注册页面：

```xml
<activity
    android:name=".AudioCompressionActivity"
    android:exported="false"
    android:label="@string/audio_compression_title" />
```

## 6. 首页入口是否需要迁移

以下改动只负责把示例入口加入本项目第五个“我的”Tab，不属于压缩工具本身：

```text
MockDataSource.java 中的 audio_compression 设置项
SettingListModule.java 中的 audio_compression 点击分发
```

迁移到其他项目时不应直接复制这两个业务文件。只需要在目标项目自己的按钮或菜单点击事件中启动 `AudioCompressionActivity`：

```java
startActivity(new Intent(context, AudioCompressionActivity.class));
```

## 7. 权限与文件访问

- 系统文件选择器通过 URI 临时授权提供输入文件，不需要 `READ_EXTERNAL_STORAGE` 或 `READ_MEDIA_AUDIO`。
- 当前示例在收到 URI 后立即压缩，临时读取授权已经足够；如果要交给 WorkManager 延迟执行，应调用 `takePersistableUriPermission()` 保留授权，或先复制到应用专属目录。
- 不要尝试把 `content://` URI 强行转换为真实文件路径；直接将 URI 交给 `AudioFileUtils` 和 `AudioCompressor`。
- 输出使用 `getExternalFilesDir(Environment.DIRECTORY_MUSIC)`，不需要写存储权限。
- 应用专属目录中的文件会在卸载应用时删除。
- Android 11 及以上版本的部分文件管理器不会直接展示 `Android/data`。若需要把文件交给其他应用，应另外配置 `FileProvider` 并提供分享功能。

## 8. 异常处理

调用方至少需要处理以下情况：

- 文件大小等于或超过 20 MiB。
- URI 已失效、无读取权限或文档提供器无法打开文件。
- 文件没有音频轨道、文件损坏或不是有效音频。
- 系统缺少源格式解码器。
- 原始 PCM 不是 16 位格式。
- 设备不支持目标 AAC 编码配置。
- 输出目录或临时文件无法创建。
- 编解码 30 秒无进展触发超时。
- 页面销毁导致的 `InterruptedIOException`。

错误后应删除 `.part` 临时文件；不要删除已经成功重命名的最终 `.m4a` 文件。

## 9. 验证清单

迁移完成后至少验证：

1. 选择小于 20 MiB 的 M4A、3GP/AMR、MP3 和 16 位 WAV，可以生成并播放 M4A。
2. 大小恰好为 20 MiB 和超过 20 MiB 的文件均被拒绝。
3. 损坏文件和伪装成音频的非音频文件不会导致崩溃。
4. 压缩期间退出或旋转页面，任务能够取消且临时文件被清理。
5. 连续压缩多个同名文件不会覆盖已有结果。
6. Android 6 到当前主流版本上至少各验证一台设备。
7. 深色模式、窄屏和长文件名下文字不重叠，输出路径可以完整查看或选择复制。

当前项目对应的 JVM 边界测试位于：

```text
app/src/test/java/com/example/myapplication/audio/AudioFileUtilsTest.java
```

建议迁移时一并复制该测试，并修改 package。
